# Labeled

The default theme: section labels in a left column, one row per parameter
with the name, the type and the default as a pill.

```@docstyle
theme = :labeled
```

```@meta
CurrentModule = Main.ConvDemo
```

```@docs; canonical=false
conv2d
channels
reset_cache!
```
