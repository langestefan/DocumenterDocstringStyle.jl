# PyData

Modeled on the PyTorch docs (pydata-sphinx-theme): shaded section bars with Python-style labels, bullets reading **name** (type) – description, and the default after the description.

```@docstyle
theme = :pydata
```

```@meta
CurrentModule = Main.ConvDemo
```

```@docs; canonical=false
conv2d
channels
reset_cache!
```
