# Rustdoc

Modeled on docs.rs (rustdoc): plain headings with a bottom border and parameter lists exactly as written.

```@docstyle
theme = :rustdoc
```

```@meta
CurrentModule = Main.ConvDemo
```

```@docs; canonical=false
conv2d
channels
reset_cache!
```
