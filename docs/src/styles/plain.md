# Plain

The demo docstrings rendered by stock Documenter, with no theme applied. The
schema check still runs on them. This is what `SchemaConfig(theme = :plain)`
produces, and the baseline the other styles are compared against.

```@meta
CurrentModule = Main.ConvDemo
```

```@docs
conv2d
Conv2d
conv_transpose2d
channels
reset_cache!
```
